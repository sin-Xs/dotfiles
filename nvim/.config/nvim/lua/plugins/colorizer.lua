return {
    'catgoose/nvim-colorizer.lua',
    -- event = "BufReadPre",
    opts = {
        user_default_options = {
            mode = 'background',
            css = true,
            css_fn = true,
        }
    }
}
